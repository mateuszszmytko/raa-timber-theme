<article class="c-article-card">
	<h2 class="c-article-card__title">
        <a href="{{post.link}}">{{ post.title }}</a>
    </h2>
	<p class="c-article-card__preview">{{ post.preview.length(20) }}</p>
</article>
